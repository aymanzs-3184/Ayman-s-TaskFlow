server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # This line makes React Router work —
        # all routes fall back to index.html
        try_files $uri $uri/ /index.html;
    }
}